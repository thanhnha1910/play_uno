services:
  - type: web
    name: uno-game
    env: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "cd backend && uvicorn main:socket_app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
